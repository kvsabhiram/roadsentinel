# Minimal dependencies to run the demo app on a laptop (CPU is fine).
#
# Install torch CPU-only first (much smaller download than the CUDA build
# used for training) -- skip this line if the laptop has an NVIDIA GPU and
# you'd rather use the matching CUDA wheel instead:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#
# Then:
#   pip install -r requirements.txt

gradio==6.27.0
ultralytics==8.3.141
rfdetr==1.10.0
opencv-python-headless==4.11.0.86
numpy==1.26.4
